import '@testing-library/jest-dom';

describe('SelectableProvider', () => {
  test.todo('accepts a default selection');
});

describe('useIsSelected', () => {
  test.todo('returns true when provided arg is selected');

  test.todo('returns false when provided arg is not selected');
});

describe('useAddToSelection', () => {
  test.todo('adds provided arg to current selections');
});

describe('useRemoveFromSelection', () => {
  test.todo('removes provided arg from current selections');
});
