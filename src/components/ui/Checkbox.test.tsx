import '@testing-library/jest-dom';

describe('Checkbox', () => {
  describe('indeterminate prop', () => {
    // useful since property is set in a useEffect
    test.todo('sets the indeterminate property on input');
  });
});
