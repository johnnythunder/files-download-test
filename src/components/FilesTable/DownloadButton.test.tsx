import '@testing-library/jest-dom';

describe('DownloadButton', () => {
  test.todo('is disabled when no selections are present');

  describe('click', () => {
    test.todo('invokes downloadFiles');
  });
});
