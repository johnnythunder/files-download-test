import '@testing-library/jest-dom';

describe('SelectAllCheckbox', () => {
  describe('click', () => {
    describe('when current selection is empty', () => {
      test.todo('selects all');
    });

    describe('when current selection is max', () => {
      test.todo('deselects all');
    });

    describe('when current selection is partial', () => {
      test.todo('selects all');
    });
  });
});
